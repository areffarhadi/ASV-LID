# Evaluation protocols

Full trial/manifest files remain on scratch (see `config/paths.sh`).

- LID manifest format: `flag<TAB>relative_wav_path<TAB>language`
  - flag 1 = train, 2 = valid1, 3 = val2
- Example lines: `protocols/manifest.example.txt`
